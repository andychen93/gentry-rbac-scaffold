# 复制为 .env 后按需修改（.env 已在 .gitignore 中）
#
# 开发环境默认走 vite.config.ts 的 proxy，把 /api 转发到 http://127.0.0.1:9090，
# 因此本地开发通常不需要配置任何变量。
#
# 前后端分离部署（前端静态托管、后端独立域名）时才需要指定后端地址：
# VITE_API_BASE_URL=https://api.example.com
