/**
 * 应用级品牌配置。
 *
 * 新项目改名字只需要动这里 + `index.html` 的 `<title>`。
 * 引用位置：Logo（侧栏）、LoginPage（登录页标题）、AppFooter（页脚）。
 */

/** 系统全名，显示在登录页标题、侧栏 Logo、页脚 */
export const APP_NAME = 'RBAC 管理平台';

/** Logo 圆形图标里的单字符缩写 */
export const APP_INITIAL = 'R';
