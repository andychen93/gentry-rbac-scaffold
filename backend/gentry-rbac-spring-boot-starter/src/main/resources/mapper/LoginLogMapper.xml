<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gentry.rbac.log.mapper.LoginLogMapper">

    <sql id="queryCondition">
        WHERE 1 = 1
        <if test="query.username != null and query.username != ''">AND username = #{query.username}</if>
        <if test="query.status != null">AND status = #{query.status}</if>
        <if test="query.startTime != null">AND login_time &gt;= #{query.startTime}</if>
        <if test="query.endTime != null">AND login_time &lt;= #{query.endTime}</if>
    </sql>

    <select id="selectList" resultType="com.gentry.rbac.log.vo.LoginLogListVO">
        SELECT id, username, login_type AS loginType, login_ip AS loginIp, location, browser, os,
            status, message, login_time AS loginTime
        FROM sys_login_log <include refid="queryCondition"/>
        ORDER BY login_time DESC
        LIMIT #{query.pageSize} OFFSET #{query.offset}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM sys_login_log <include refid="queryCondition"/>
    </select>

    <select id="selectExportList" resultType="com.gentry.rbac.log.vo.LoginLogListVO">
        SELECT id, username, login_type AS loginType, login_ip AS loginIp, location, browser, os,
            status, message, login_time AS loginTime
        FROM sys_login_log <include refid="queryCondition"/>
        ORDER BY login_time DESC
        LIMIT 10000
    </select>

</mapper>
