<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.precision.rbac.role.mapper.RoleMenuMapper">

    <select id="selectAllMenuIds" resultType="java.lang.Long">
        SELECT id FROM sys_menu WHERE deleted = 0
    </select>

    <insert id="batchInsertRoleMenu" parameterType="java.util.List">
        INSERT INTO sys_role_menu (id, role_id, menu_id, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.roleId}, #{item.menuId}, NOW())
        </foreach>
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO sys_role_menu (id, role_id, menu_id, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.roleId}, #{item.menuId}, NOW())
        </foreach>
    </insert>

    <delete id="deleteByMenuIds">
        DELETE FROM sys_role_menu WHERE menu_id IN
        <foreach collection="menuIds" item="menuId" open="(" separator="," close=")">
            #{menuId}
        </foreach>
    </delete>

    <select id="selectPermissionsByRoleIds" resultType="java.lang.String">
        SELECT DISTINCT m.permission
        FROM sys_role_menu rm
        JOIN sys_menu m ON rm.menu_id = m.id
        JOIN sys_role r ON rm.role_id = r.id
        WHERE rm.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        AND r.deleted = 0
        AND r.status = 1
        AND m.deleted = 0
        AND m.permission IS NOT NULL
        AND m.permission != ''
    </select>

</mapper>
