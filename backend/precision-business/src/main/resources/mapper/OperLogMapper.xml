<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.precision.rbac.log.mapper.OperLogMapper">

    <sql id="queryCondition">
        WHERE tenant_id = #{tenantId}
        <if test="query.module != null and query.module != ''">AND module LIKE CONCAT('%', #{query.module}, '%')</if>
        <if test="query.type != null and query.type != ''">AND type = #{query.type}</if>
        <if test="query.operator != null and query.operator != ''">AND operator LIKE CONCAT('%', #{query.operator}, '%')</if>
        <if test="query.status != null">AND status = #{query.status}</if>
        <if test="query.startTime != null">AND operate_time &gt;= #{query.startTime}</if>
        <if test="query.endTime != null">AND operate_time &lt;= #{query.endTime}</if>
    </sql>

    <select id="selectList" resultType="com.precision.rbac.log.vo.OperLogListVO">
        SELECT id, module, type, title, operator, operator_ip AS operatorIp, location, status, cost_time AS costTime, operate_time AS operateTime
        FROM sys_oper_log <include refid="queryCondition"/>
        ORDER BY operate_time DESC
        LIMIT #{query.pageSize} OFFSET #{query.offset}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM sys_oper_log <include refid="queryCondition"/>
    </select>

    <select id="selectExportList" resultType="com.precision.rbac.log.vo.OperLogListVO">
        SELECT id, module, type, title, operator, operator_ip AS operatorIp, location, status, cost_time AS costTime, operate_time AS operateTime
        FROM sys_oper_log <include refid="queryCondition"/>
        ORDER BY operate_time DESC
        LIMIT 10000
    </select>

</mapper>
