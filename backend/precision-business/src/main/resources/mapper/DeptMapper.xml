<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.precision.rbac.dept.mapper.DeptMapper">

    <resultMap id="DeptResultMap" type="com.precision.rbac.dept.entity.Dept">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="ancestors" column="ancestors"/>
        <result property="name" column="name"/>
        <result property="leaderId" column="leader_id"/>
        <result property="leaderName" column="leader_name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <sql id="queryCondition">
        WHERE d.tenant_id = #{tenantId} AND d.deleted = 0
        <if test="query.name != null and query.name != ''">
            AND d.name LIKE CONCAT('%', #{query.name}, '%')
        </if>
        <if test="query.status != null">
            AND d.status = #{query.status}
        </if>
    </sql>

    <select id="selectList" resultMap="DeptResultMap">
        SELECT d.* FROM sys_dept d
        <include refid="queryCondition"/>
        ORDER BY d.sort ASC, d.create_time ASC
    </select>



    <select id="countUsersByTenantGroupByDept" resultType="map">
        SELECT dept_id AS deptId, COUNT(*) AS cnt
        FROM sys_user
        WHERE tenant_id = #{tenantId} AND deleted = 0
        GROUP BY dept_id
    </select>

</mapper>
